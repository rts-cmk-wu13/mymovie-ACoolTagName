let sectionElm = document.creatElement("section")
sectionElm.class = "pokelist"

fetch("/data/pokemon.json")
	.then(function (response) {
	return response.json()
}).then(
	function(data) {
		let divElm =document.creatElement("div")
		divElm.innerHTML = data.map(function(pokemon){
		
		let id = pokemonurl.slice(0, -1).split("/").pop()

		return `
		<article>
			<img src="${}" alt="">
			<h2>${pokemon.name}</h2>
		</article>
		`

		}).join("")
		divElm.append(pokelist);
		sectionElm.append(divElm)
})

sectionElm.innerHTML = `


`

document.querySelector("main").append(sectionElm)


//
function createCard(pokemon) {

		return `
		<article>
			<h2>${pokemon.name}</h2>
		</article>
		`
}
